# Astrionyx-deploy

`Astrionyx` 的公开部署仓库，存放 GitHub Actions 工作流与发布配置。

---

## 这个仓库做什么

1. 接收私有仓库的触发请求
2. 拉取 `Astrionyx` 指定提交的源码
3. 构建后端 API Docker 镜像
4. 推送镜像到 GHCR
5. 通过 SSH 部署到目标服务器

## 为什么要单独开一个公开仓库

私有仓库的 Actions 免费时长有限，公开仓库没有这个限制。

所以把耗时的构建过程放到这里跑，私有仓库只做轻量触发，源码不对外暴露。
