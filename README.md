# deploy

存放 GitHub Actions 工作流与发布配置的公开部署仓库。

---

## 这个仓库做什么

接收来自私有仓库的触发请求，执行构建与部署流程。

1. 接收私有仓库的触发请求，校验来源合法性
2. 拉取私有仓库指定提交的源码
3. 执行构建流程（Docker 镜像 / 前端产物等）
4. 将构建产物推送到目标存储或镜像仓库
5. 通过 SSH 部署到目标服务器

---

## 为什么要单独开一个公开仓库

私有仓库的 Actions 免费时长有限，公开仓库没有这个限制。  
所以把耗时的构建过程放到这里跑，私有仓库只做轻量触发，源码不对外暴露。
